import { createTheme } from '@mui/material';

const theme = createTheme({
  
  palette: {
    type: 'dark',
    primary: {
      main: '#717dbb',
    },
    secondary: {
      main: '#fd2882',
    },
  },
});

export default theme;
